export interface WindowProperties {
  isXXS: boolean;
  isXS: boolean;
  isS: boolean;
  isM: boolean;
  isL: boolean;
  isXL: boolean;
}

export interface SidenavProperties {
  opened: boolean;
  mode: string;
  lockedinclosed: boolean;
}

export interface FormDigestValue {
    FormDigestTimeoutSeconds: number;
    FormDigestValue: string;
    LibraryVersion: string;
    SiteFullUrl: string;
    SupportedSchemaVersions: any[];
    WebFullUrl: string;
    odata_metadata?: string;
}

export interface SpImage {
    ArrayBuffer: ArrayBuffer;
    Filename: string;
}

export interface SpImageFile {
    ArrayBuffer: ArrayBuffer;
    Filename: string;
}


